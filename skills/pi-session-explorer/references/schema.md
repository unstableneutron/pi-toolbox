# pi-session-explorer schema (bootstrap)

This bootstrap creates placeholder SQL files for the future DuckDB schema.

Planned core objects:
- `session_files`
- `entries`
- `messages` view
- `tool_calls` view

See `sql/schema.sql` and related query files for implementation placeholders.
